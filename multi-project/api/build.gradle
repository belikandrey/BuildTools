plugins {
    id 'java'
}

group 'com.epam'
version '1.0'

repositories {
    mavenCentral()
}

project.dependencies{
    compile(project(':core'))
}

dependencies {
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
}

test {
    useJUnitPlatform()
}